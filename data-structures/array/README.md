# Array

Dynamic array implementation in C++ and Java.

## Purpose

Understanding how dynamic arrays work internally (like vector / ArrayList).

## Key Idea

When capacity is full, array size is doubled and elements are copied to a new memory block.

## Complexity

- Access: O(1)
- Insert (amortized): O(1)
- Resize: O(n)
